# 默认播放设备   这里用的端口号是2，设备的端口号可以用arecord -l 和 aplay -l查看
pcm.!default {
    type plug	
    slave {
        pcm "hw:2,0"
    }
}

# 默认录音设备
pcm.!dsnoop {
    type dsnoop
    ipc_key 1024
    slave {
        pcm "hw:2,0"
    }
}

# 默认控制设备
ctl.!default {
    type hw
    card 2
}